spring:
  application:
    name: payment-api

server:
  port: 9000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9090/eureka/
    registerWithEureka: true
    fetchRegistry: true
