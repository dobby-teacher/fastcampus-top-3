apiVersion: v1
kind: Service
metadata:
  name: next-graphql
  labels:
    app: next-graphql
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: next-graphql
